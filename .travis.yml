language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install


#before_script:
  #- npm install -g gulp

script:
  - npm run build

after_script:
# E: Build LifeCycle

branches:
  only:
    - master
env:
 global:
   - GH_REF: github.com:Fukai-AC/PersonalHistoryRecord.git